<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.hdu.mapper.OrderItemMapper">

    <insert id="insertOrderItem" useGeneratedKeys="true" keyProperty="itemId">
        INSERT INTO order_item (order_id, book_id, price_snapshot, quantity)
        VALUES (#{orderId}, #{bookId}, #{priceSnapshot}, #{quantity})
    </insert>

    <select id="findByOrderId" resultType="OrderItem">
        SELECT item_id, order_id, book_id, price_snapshot, quantity
        FROM order_item
        WHERE order_id = #{orderId}
    </select>

</mapper>
