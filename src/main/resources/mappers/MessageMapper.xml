<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.hdu.mapper.MessageMapper">

    <insert id="insertMessage" useGeneratedKeys="true" keyProperty="msgId">
        INSERT INTO message (from_user_id, to_user_id, book_id, content, is_read)
        VALUES (#{fromUserId}, #{toUserId}, #{bookId}, #{content}, #{isRead})
    </insert>

</mapper>
